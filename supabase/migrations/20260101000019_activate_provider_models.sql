-- ============================================================================
-- 迁移 0019：恢复按提供商选择所需的内置模型目录
--
-- 0012 曾为「仅有 SiliconFlow 密钥」的本地联调临时下架其它提供商模型。现在前端会按用户
-- 已启用的提供商和节点模态过滤模型，因此无需再用全局下架规避缺少密钥；重新上架后，未配置
-- 对应提供商的用户仍不会在节点中看到这些模型。
-- ============================================================================

update public.model_catalog
set is_active = true
where key in ('gpt-image-2', 'nano-banana-pro', 'seedance-2.0', 'seedream-3.0');
